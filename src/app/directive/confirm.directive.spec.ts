import { ConfirmeDirective } from './confirme.directive';

describe('ConfirmeDirective', () => {
  it('should create an instance', () => {
    const directive = new ConfirmeDirective();
    expect(directive).toBeTruthy();
  });
});
